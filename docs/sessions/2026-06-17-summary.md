# Session Summary — 2026-06-17

## Project: fedora-proart-kickstart

### What was accomplished

#### Phase 3 complete — MCP server rewrite committed

`hermes/mcp/openwebui-mcp.py` v2.0 committed (fcf60a4). Full rewrite from the v1 single-KB stub to a four-tier RAG-aware MCP server:

- Five tools: `rag_search`, `rag_add_doc`, `rag_add_issue`, `rag_index_project`, `rag_list_kbs`
- Dynamic KB resolution — discovers all KBs at runtime, no hardcoded IDs
- Auto-creates missing KBs on first use via `POST /api/v1/knowledge/create`
- CWD-based framework detection (Drupal/Symfony/WordPress/CakePHP/Laravel from composer.json)
- OS detection for Tier 4 context (`linux-fedora`, `macos`, `windows`)
- `rag_index_project` walks project tree, skips vendor/var/node_modules/core, uploads .php/.twig/.yml/.yaml/.env.example/.md files
- Upsert semantics: same-name file is removed and re-uploaded on each call

#### CLAUDE.md updated — four-tier RAG rules

Replaced the old flat RAG section with:
- Four-tier table (framework / project / common-issues / devops-general)
- Excluded paths (vendor/, web/core/, node_modules/, var/cache/)
- Search-first rule with default tiers `["framework","project","common-issues"]`
- OS context rule: Tier 4 only
- Mandatory session summary rule

#### RAG usage documentation created

`ai-stack/README-rag.md` (fe2111f) — complete guide covering:
- How to start a new project (`rag_index_project`)
- Search patterns with tier selection examples
- `rag_add_issue` and `rag_add_doc` with tag conventions
- `rag_list_kbs` for inspection
- Hermes, Claude Code, and Cursor registration instructions
- Session workflow
- Excluded paths
- Troubleshooting (no KBs found, expired token, stale results, Hermes timeout)

`ai-stack/cursor-mcp.dist.json` — Cursor MCP template to copy per-project.

#### Claude Code MCP registered

```bash
claude mcp add -s user openwebui-rag \
  -e OPENWEBUI_URL=http://localhost:3000 \
  -e OPENWEBUI_TOKEN="<token>" \
  -e RAG_CWD_DETECT=1 \
  -- python3 /home/devuser/fedora-build/hermes/mcp/openwebui-mcp.py
```

Confirmed connected: `claude mcp list` shows `openwebui-rag: ✔ Connected`.

End-to-end test via direct stdin confirmed `rag_search` returns the stored CUDA issue from `common-issues` KB with relevance 0.185.

MCP tools load at session start — fresh Claude Code session required to use them interactively.

### Key bugs fixed in v2.0

| Bug | Fix |
|-----|-----|
| `POST /api/v1/knowledge/` → HTTP 405 | Correct endpoint: `/api/v1/knowledge/create` |
| `detail.get("files", [])` raises TypeError | `files` field is `None` not missing; use `detail.get("files") or []` |

### Four-tier structure (reference)

| Tier | KB name | Contents |
|------|---------|----------|
| 1 | `framework-{name}` | PHP framework/CMS reference |
| 2 | `project-{slug}` | Per-project source + project-specific devops |
| 3 | `common-issues` | Cross-cutting gotchas, bugs, non-obvious fixes |
| 4 | `devops-general` | Docker, k8s, Linux, nginx, SSL, OS patterns |

### Pending

- **Phases 1, 2, 4, 5**: Qdrant migration, seeding Tier 1 framework docs, index-project.sh rewrite, Hermes context/hooks update
- **Language tier**: User raised PHP/Bash as possible Tier 5 — recommended tags instead; not closed
- **Token expiry**: JWT in Hermes config and Claude Code MCP expires ~2026-08-07; update both when expired
