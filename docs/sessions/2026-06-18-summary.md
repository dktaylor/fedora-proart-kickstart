# Session Summary — 2026-06-18

## What happened

This session completed the RAG stack data-persistence fix that was started in the prior session (context ran out mid-task). The prior session had done the heavy lifting (bind-mount migration, auto-reindex, stale volume removal) but was blocked from pushing because verify.sh still had 5 failures.

### Remaining failures resolved

1. **`qwen2.5-coder:7b-instruct-q5_K_M` not installed**
   - Prior background pull had been interrupted (context window compacted mid-download).
   - `ollama list` showed only `q4_K_M` and `q8_0`.
   - Re-ran `ollama pull qwen2.5-coder:7b-instruct-q5_K_M` — completed in seconds because the large layer was already cached from the prior partial download.

2. **Step 44 ollama-backend scripts missing** (`ollama-backend`, `ollama-local`, `ollama-remote`, `/etc/ollama-backend.conf`)
   - Step 44 was never run on this machine during the original install.
   - Extracted lines 1657–2131 from `scripts/fedora-postinstall-setup.sh` into `/tmp/install-step44.sh`.
   - User ran `sudo bash /tmp/install-step44.sh` — all 4 files created.

### verify.sh result

```
PASS: 65    FAIL: 0    SKIP: 6
All present checks passed.
```

### Committed and pushed

Commit `84c642c` — "Fix RAG stack data persistence and verify.sh checks"

Files changed:
- `kickstart/fedora-ks-auto.cfg` — removed 3 stale `-v open-webui:/app/backend/data` fallbacks
- `kickstart/fedora-ks-manual.cfg` — same
- `scripts/fedora-postinstall-setup.sh` — step 46 comment updated; same 3 stale fallbacks removed
- `scripts/verify.sh` — rag CLI, data dirs, ollama-backend scripts checked; container check changed from FAIL to SKIP (on-demand stack)

## Context: what the prior session did

(Summarized here for completeness since it was committed in this push.)

- **Root cause of repeated data loss**: Docker named volumes at `/var/lib/docker/volumes/` are destroyed by `docker volume prune` or `compose down -v`. Fixed by switching to host bind mounts at `/opt/rag-stack/data/`.
- **SELinux**: bind mounts on Fedora require `:z` label or writes get AVC denied.
- **Migration**: used Alpine container with `:z` on both sides to copy Qdrant (694 files) and WebUI (345 files) data before deleting old volumes.
- **Auto-reindex**: `rag start` now checks `open-webui_knowledge` point count; if 0, re-indexes all paths from `indexes.conf`. `rag reindex` command added for manual trigger.
- **`indexes.conf`**: `/opt/rag-stack/indexes.conf` seeds both projects: `fedora-proart-kickstart` and `rag-stack`. Never overwritten by `install.sh`.
- **MCP `rag_index_project`**: after a successful index, appends path to `indexes.conf` automatically.

## Current state

- RAG stack running: Qdrant + Open WebUI on bind mounts, data at `/opt/rag-stack/data/`
- All verify.sh checks pass (65/65)
- `fedora-proart-kickstart` repo is clean and pushed to main
- Model `qwen2.5-coder:7b-instruct-q5_K_M` installed (5.4 GB)

## Pending (non-blocking)

- Set `DESKTOP_IP` in `/etc/ollama-backend.conf` (needs `tailscale ip -4` on desktop)
- Optional: automated backup of `/opt/rag-stack/data/`
- Optional: clean up duplicate `project-fedora-proart` KB naming inconsistency in Open WebUI
