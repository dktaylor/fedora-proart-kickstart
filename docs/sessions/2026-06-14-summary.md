# Session Summary — 2026-06-14

## Project: fedora-proart-kickstart

### What was accomplished

#### Part 1 (earlier session)
- Docker firewalld ZONE_CONFLICT fix added to all provisioning files
- Open WebUI container created and RAG knowledge base initialized (12 files)
- GitHub repo created: github.com/dktaylor/fedora-proart-kickstart
- Project directory restructured: kickstart/, scripts/, testing/, hermes/, docs/
- VM kickstart (fedora-ks-vm.cfg) generated with hardware step guards
- KVM test runner (testing/test-vm.sh) created
- build-fedora-ks-iso.sh updated with vm mode and new paths

#### Part 2 (this session) — Hermes Phase 1 complete

Hermes v0.16.0 installed and configured as local AI orchestrator.

**Dual-model configuration** (`~/.hermes/config.yaml`)
- Default provider: `custom` (local Ollama at `http://localhost:11434/v1`)
- Default model: `qwen2.5-coder:7b-instruct-q4_K_M` (~6s responses)
  - Switched from q8_0 → q4_K_M: frees VRAM for 64K KV cache
- `context_length: 65536` + `ollama_num_ctx: 65536` to satisfy Hermes 64K minimum
- Model aliases:
  - `brain` → `claude-opus-4-8` (direct Anthropic API)
  - `claude` → `claude-sonnet-4-6` (direct Anthropic API)
  - `local` → `qwen2.5-coder:7b-instruct-q4_K_M`
- `ANTHROPIC_API_KEY` in `~/.hermes/.env` (requires console.anthropic.com credits)

**Usage**
```bash
hermes -z "your task"           # local Ollama, fast, routine tasks (~6s)
hermes -m brain -z "your task"  # Claude Opus, complex decisions
hermes                          # interactive session
```

**Troubleshooting**
- `provider: "ollama"` not recognized in v0.16.0 — use `"custom"` instead
- Hermes enforces 64K context minimum — override with `context_length` + `ollama_num_ctx`
- q8_0 (8GB VRAM) left no room for KV cache at 64K → q4_K_M (~4GB) fixes this
- Claude Code OAuth token (`sk-ant-oat01-`) ≠ Anthropic API key (`sk-ant-api03-`)
- Anthropic API requires separate billing at console.anthropic.com

**New files**
| File | Purpose |
|------|---------|
| `hermes/context.md` | Project context injected into every Hermes session |
| `hermes/setup-hermes.sh` | Idempotent provisioning script (step 45) |

**Step 45 added** to all three provisioning files (postinstall-setup.sh,
fedora-ks-auto.cfg, fedora-ks-manual.cfg) — calls setup-hermes.sh which
installs ripgrep + Hermes, patches config.yaml, copies context.md, sets
ANTHROPIC_API_KEY placeholder.

### Pending

- Add Anthropic API credits (console.anthropic.com) to enable `brain` alias
- Phase 2: `hermes/mcp/openwebui-mcp.py` — MCP server bridging Hermes → RAG
- `testing/test-postinstall.sh` — Podman container test runner
- Wire `hermes/context.md` as system prompt injection via Hermes config

### RAG files updated

- `01-sessions--2026-06-14-summary.md` (this file)
- `00-overview--CLAUDE.md`
- `02-kickstart--fedora-ks-auto.cfg`
- `02-kickstart--fedora-ks-manual.cfg`
- `03-scripts--postinstall-setup.sh`
- `05-hermes--context.md` (new)
- `05-hermes--setup-hermes.sh` (new)
